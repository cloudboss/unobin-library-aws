# Examples

These examples are complete Unobin projects that demonstrate AWS library services.
Each subdirectory has its own `project.ub`, stack file, and README.

- [lambdamicrovms-http](lambdamicrovms-http/) creates a Lambda MicroVM image,
  starts a MicroVM, sends an authenticated HTTP request, and terminates it.
